<h1>Spaces#index</h1>
<p>Find me in app/views/spaces/index.html.erb</p>

<div class="container">
  <div class="row">
    <% @spaces.each do |space|%>
    <%= link_to(space) do %>
       <%= render 'shared/card', space: space %>
    <% end %>


<% end %>
</div>
</div>
